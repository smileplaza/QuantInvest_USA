# 자주 묻는 질문 (FAQ)

QuantInvest Tool 사용 중 자주 발생하는 질문과 해결책을 정리했습니다.
앱 내에서도 **도움말 > 자주 묻는 질문 (FAQ)** 메뉴 또는 `F1` 키로 확인할 수 있습니다.

---

## 데이터

### Q. 데이터 다운로드가 실패합니다.
인터넷 연결, 티커 심볼(예: `AAPL`), 날짜 범위를 확인하세요.

- Yahoo Finance가 해당 종목/기간 데이터를 제공하지 않을 수 있습니다.
- 시작 날짜는 종료 날짜보다 앞서야 합니다.
- 2025년 2월 Yahoo Finance 형식 변경에 대응하려면 `yfinance >= 1.5.2`가 필요합니다.

### Q. 다운로드한 데이터는 어디에 저장되나요?
SQLite 캐시(`quantinvest_cache.db`)에 저장되어 동일 종목/기간을 다시 받지 않습니다.
**주식 및 날짜 설정 탭 > 캐시 삭제**로 초기화할 수 있습니다.

---

## 백테스트 / 전략

### Q. "데이터가 부족합니다" 오류가 납니다.
전략 파라미터(기간)가 데이터 길이보다 큰 경우입니다. 예를 들어 60일 데이터에
100일 이동평균은 계산할 수 없습니다. **파라미터를 줄이거나 더 긴 날짜 범위**를
선택하세요.

### Q. 4개 전략은 어떻게 다른가요?
| 전략 | 핵심 로직 |
|------|----------|
| 모멘텀 | 가격 상승 추세 + 자금 흐름 지수(MFI) 필터 |
| 추세 추종 | 단기/장기 지수이동평균(EMA) 교차 |
| 평균 회귀 | Z-점수 기반 통계적 반전 |
| 포트폴리오 | 다종목 모멘텀 프록시 |

### Q. 결과가 원본 교재(노트북)와 다릅니다.
- **추세 추종**: 원본 노트북과 0.1% 이내로 일치합니다.
- **모멘텀**: MFI 필터가 추가된 확장 구현입니다.
- **평균 회귀 / 포트폴리오**: 단일 종목용으로 단순화된 버전입니다.

자세한 내용은 `.claude/knowledge/NOTEBOOK_VALIDATION.md`를 참고하세요.

---

## 최적화

### Q. 최적화가 너무 오래 걸립니다.
그리드 서치는 조합 수에 비례해 느려집니다.

- 파라미터 범위를 좁히거나 단계(step)를 키우세요.
- **설정(Ctrl+,)에서 병렬 처리**를 켜면 여러 CPU 코어로 분산되어 빨라집니다.

참고 성능(5년 데이터 기준): 100개 조합 ≈ 0.5초, 1000개 조합 ≈ 5초.

---

## 설정 / 저장

### Q. 초기 자본, 수수료를 바꿀 수 있나요?
**설정(Ctrl+,)** 에서 초기 자본, 거래 수수료율, 무위험 이율, 테마(다크/라이트),
병렬 처리 기본값을 변경할 수 있으며 자동으로 저장됩니다.

### Q. 결과를 어떻게 저장하나요?
결과 분석 탭에서:
- **CSV로 내보내기** (Ctrl+E) — 시계열 데이터
- **보고서 저장** — 텍스트 요약
- **PDF로 내보내기** (Ctrl+P) — 지표 요약 + 차트가 포함된 리포트

차트 창에서는 개별 이미지(PNG) 저장도 가능합니다.

---

## 단축키

| 단축키 | 동작 |
|--------|------|
| `Ctrl+D` | 데이터 다운로드 |
| `Ctrl+R` | 백테스트 실행 |
| `Ctrl+Shift+O` | 최적화 시작 |
| `Ctrl+E` | CSV 내보내기 |
| `Ctrl+P` | PDF 내보내기 |
| `Ctrl+,` | 설정 |
| `Ctrl+1` ~ `Ctrl+4` | 탭 이동 |
| `F1` | 이 도움말 |
| `Ctrl+Q` | 종료 |

---

## 빌드 / 배포

### Q. .exe가 실행되지 않습니다.
- 모든 의존성이 `requirements.txt`에 포함되어 있는지 확인하세요.
- `pyinstaller --clean QuantInvest.spec`로 캐시를 정리하고 재빌드하세요.
- OS별 상세 가이드: `INSTALL_GUIDE_WINDOWS.md`, `INSTALL_GUIDE_MACOS.md`, `INSTALL_GUIDE_LINUX.md`

### Q. PDF 리포트의 지표 라벨이 영어입니다.
matplotlib 기본 폰트에 한글 글리프가 없어, 배포 환경에서 깨지지 않도록 PDF
리포트는 영문 라벨을 사용합니다. 앱 화면(결과 테이블)은 한글로 표시됩니다.
